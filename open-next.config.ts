// ABOUTME: OpenNext configuration for Cloudflare Workers deployment.
// ABOUTME: Defines how Next.js is adapted for the Cloudflare runtime.

import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig({});
